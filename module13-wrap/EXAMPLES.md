# Examples — SV TB complete

Wrap-up. Recap what you can do; name the next course on the ladder.

## Prompts

1. Restate the core idea of **SV TB complete** in one sentence.
2. In your own words, capture this beat: *You started with testbench anatomy and self-checking patterns, walked clock and reset, tasks and fork-join, file vectors, virtual interfaces, classes, constrained random lite, coverpoints, SVA timelines, and the map…*
3. Optional: map the same idea to a tool or flow you already use.

## Stretch

Open the next course README and write one goal for your first module there.
