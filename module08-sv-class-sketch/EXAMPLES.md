# Examples — Class / inheritance sketch

Track A prompts. Browser lab **`sv-class-sketch`** is shipped.

## Prompts

1. Restate the core idea of **Class / inheritance sketch** in one sentence.
2. In your own words, capture this beat: *SystemVerilog testbenches lean on classes long before UVM. Two trees matter: objects hold transaction data you construct with new and pass by handle; components live in a hierarchy with build and run roles.…*
3. Sketch a base transaction class and one extended type with one extra field.
4. Say what `super.new` is for in one sentence.

## Stretch

When ready, redo the same idea in the **`sv-class-sketch`** starter challenges.
