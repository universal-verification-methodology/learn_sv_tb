# Examples — Self-checking TB

Track A prompts. Browser lab **`self-check-tb`** is shipped.

## Prompts

1. Restate the core idea of **Self-checking TB** in one sentence.
2. In your own words, capture this beat: *A self-checking testbench compares what the DUT produced against an expected value you computed separately—then reports pass or fail without you staring at every wave. The flow is stimulus, sample outputs after a…*
3. Write stimulus → reference expect → compare for and2 with a=1,b=1,y=1.
4. Name what goes wrong if you only `$display` without comparing.

## Stretch

When ready, redo the same idea in the **`self-check-tb`** starter challenges.
